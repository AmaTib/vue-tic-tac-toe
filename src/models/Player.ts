export class Player {
  /* playerX: boolean; */
  playerSymbol: string;

  constructor(public playerName: string, playerSymbol: string) {
    /* this.playerX = true; */
    this.playerSymbol = playerSymbol;
  }
}
